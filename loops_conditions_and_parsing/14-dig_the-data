#!/usr/bin/env bash
# Script: group and sort Apache log visitors by IP and HTTP code

awk 'NF>=9 {count[$1" "$9]++} END {for (pair in count) printf "%d %s\n", count[pair], pair}' apache-access.log | sort -nr

